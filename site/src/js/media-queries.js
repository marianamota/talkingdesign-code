// Generated by CoffeeScript 1.3.3
(function() {
  var qry1, qry2, qry3;

  $('.dimensions').html('Width: ' + $(window).width() + 'px');

  $(window).resize(function() {
    return $('.dimensions').html('Width: ' + $(this).width() + 'px');
  });

  var moveRelatedReads = {
    move: function() {
      var related = $('.related-content').clone();
      $('.related-content').hide();
      $('.article-aside').append(related);
    },
    destroy: function() {
      $('.article-aside .related-content').remove();
      $('.related-content').show();
    } 
  }

  qry1 = Harvey.attach('screen and (min-width:500px)', {
    setup: function() {
      return console.log('SETUP', this.condition);
    },
    on: function() {
      moveRelatedReads.destroy();
      return console.log('ON', this.condition);
    },
    off: function() {
      return console.log('OFF', this.condition);
    }
  });

  qry2 = Harvey.attach('screen and (min-width:870px)', {
    setup: function() {
      return console.log('SETUP', this.condition);
    },
    on: function() {
      moveRelatedReads.move();
      if ($('.article-aside.sticky').length > 0) {
        $('.article-aside.sticky').stick_in_parent({
          offset_top: 73,
        });
      }
      return console.log('ON', this.condition);
    },
    off: function() {
      moveRelatedReads.destroy();
      if ($('.article-aside.sticky').length > 0) {
        $('.article-aside.sticky').trigger('sticky_kit:detach');
      };
      return console.log('OFF', this.condition);
    }
  });

  qry3 = Harvey.attach('screen and (min-width:1024px)', {
    setup: function() {
      return console.log('SETUP', this.condition);
    },
    on: function() {
      if ($('.article-aside.sticky').length > 0) {
        $('.article-aside.sticky').stick_in_parent({
          offset_top: 73,
        });
      }
      return console.log('ON', this.condition);
    },
    off: function() {
      return console.log('OFF', this.condition);
    }
  });

}).call(this);